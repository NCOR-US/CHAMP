import { moduleForComponent, test } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';

moduleForComponent('projection-select', 'Integration | Component | projection select', {
    integration: true
});

test('it renders', function(assert) {
    assert.expect(0);
});
