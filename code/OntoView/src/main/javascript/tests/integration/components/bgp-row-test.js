import { moduleForComponent, test } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';

moduleForComponent('bgp-row', 'Integration | Component | bgp row', {
    integration: true
});

// this needs quite a bit from a canned query

test('it renders', function(assert) {
    // this.render(hbs`{{bgp-row}}`);
    // assert.equal(this.$().text().trim(), '');
    assert.expect(0);
});

test("it responds to external changes to the query", function(assert) {


    assert.expect(0);
});

test("it toggles subject editing panel", function(assert) {


    assert.expect(0);
});

test("it toggles predicate editing panel", function(assert) {


    assert.expect(0);
});

test("it toggles object editing panel", function(assert) {


    assert.expect(0);
});

test("it toggles usage of a variable", function(assert) {


    assert.expect(0);
});

test("it toggles usage of a type restriction", function(assert) {


    assert.expect(0);
});