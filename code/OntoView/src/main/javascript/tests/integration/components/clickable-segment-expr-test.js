import { moduleForComponent, test } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';

moduleForComponent('clickable-segment-expr', 'Integration | Component | clickable segment expr', {
    integration: true
});

test("it doesn't render by itself", function(assert) {

    // ensure this doesn't work since it needs an expression
    // assert.throws(() => this.render(hbs`{{clickable-segment-expr}}`));

    assert.expect(0);
});

test("it renders a full clickable expression", function(assert) {


    assert.expect(0);
});

test("it sets the current editing segment", function(assert) {


    assert.expect(0);
});
