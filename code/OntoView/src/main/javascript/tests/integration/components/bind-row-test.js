import { moduleForComponent, test } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';

moduleForComponent('bind-row', 'Integration | Component | bind row', {
    integration: true
});

test("it doesn't render by itself", function(assert) {
    // ensure this doesn't work since it needs a functionRegistry
    // assert.throws(() => this.render(hbs`{{bind-row}}`));

    assert.expect(0);
});

test("it toggles editing mode", function(assert) {


    assert.expect(0);
});
